{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/application-ext": "${workspaceRoot}/containers/php-fpm-external",
                "/application-int": "${workspaceRoot}/containers/php-fpm-internal"
            }
        }
    ]
}